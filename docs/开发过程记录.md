# 开发过程记录

本文档用于记录毕业设计开发过程中的阶段推进、技术选择、问题定位、解决方案和验证证据。与 `开发记录.md` 不同，本文档更偏向过程性说明，便于后续整理论文、答辩材料和中期检查材料。

## 2026-05-12 阶段一：项目方向确定与可行性验证

### 1. 背景与目标

最初我将项目方向确定为“企业 ICT 智能运维与故障诊断平台”。我的毕业设计目标不是只完成静态页面或简单增删改查，而是尽量体现真实运维场景，包括服务器、网络、监控、告警、AI 诊断等内容。

因此本阶段的核心目标不是马上堆业务页面，而是先验证本机是否具备完成该项目的基础条件：

1. 是否可以运行 Java 后端。
2. 是否可以运行 Vue 前端。
3. 是否可以运行 Python/FastAPI AI 服务。
4. 是否可以运行 Docker 观测组件。
5. 是否可以接入真实或近真实的运维采集对象。

### 2. 技术路线确认

最终采用的技术路线如下：

| 模块 | 技术选择 | 选择原因 |
|---|---|---|
| 前端 | Vue 3 + Vite + Element Plus | 开发效率高，适合后台管理系统和监控面板 |
| 后端 | Spring Boot 3 + JDK 17 + Maven | 企业项目常见技术栈，适合毕业设计后端主体 |
| AI 服务 | Python + FastAPI | 便于后续接入大模型 API、RAG 和故障诊断逻辑 |
| 缓存 | Redis | 可用于会话、告警状态、热点数据缓存 |
| 监控 | Prometheus + Exporter | 运维监控领域成熟方案，真实可验证 |
| 真实节点 | WSL2 Ubuntu + node_exporter | 不依赖额外实体服务器，也能采集真实 Linux 指标 |
| 容器环境 | Docker Desktop + Docker Compose | 便于统一启动 Redis、Prometheus、Blackbox 等组件 |

### 3. 可行性判断

经过检查，本机满足当前项目开发条件：

1. BIOS 虚拟化已开启。
2. Docker Desktop 可用。
3. WSL2 Ubuntu 24.04 可用。
4. JDK17 和 Maven 可用。
5. Node.js/npm 可运行 Vue 项目。
6. Python 虚拟环境可安装 FastAPI 依赖。

结论：可以按照“真实 ICT 智能运维平台”方向继续作为毕业设计项目推进。

## 2026-05-12 阶段二：基础环境搭建

### 1. JDK 与 Maven 固定

开发过程中发现 Maven 放在中文路径下时，PowerShell 输出和部分命令显示容易出现乱码。为降低后续构建风险，将 Maven 固定到英文路径：

```text
E:\Acodex\tools\apache-maven-3.9.9
```

并创建项目脚本：

```text
set-env.ps1
```

用于统一设置：

```text
JAVA_HOME=C:\Program Files\Eclipse Adoptium\jdk-17.0.18.8-hotspot
MAVEN_HOME=E:\Acodex\tools\apache-maven-3.9.9
```

验证方式：

```powershell
powershell -ExecutionPolicy Bypass -File .\set-env.ps1
mvn -pl backend -DskipTests package
```

验证结果：

```text
Apache Maven 3.9.9
Java version: 17.0.18
BUILD SUCCESS
```

### 2. Docker 观测栈搭建

使用 Docker Compose 启动以下组件：

1. Redis。
2. Prometheus。
3. Blackbox Exporter。
4. Docker node_exporter。

相关文件：

```text
deploy/docker-compose.yml
deploy/prometheus.yml
deploy/blackbox.yml
deploy/start-observability.ps1
deploy/check-observability.ps1
```

验证结果：

```text
Redis: PONG
Prometheus: Ready
node-exporter: up=1
blackbox-http: up=1
```

### 3. 过程问题

Docker Prometheus 尝试直接采集 WSL 节点时，出现容器到 WSL NAT 网络不稳定的问题。由于当前环境不方便使用管理员权限配置 `netsh portproxy`，因此没有强行修改系统网络，而是选择在 WSL 内单独部署 Prometheus。

这个方案的优点：

1. WSL 内 Prometheus 可以稳定采集本机 Linux 指标。
2. Spring Boot 后端可以通过 WSL IP 查询 WSL Prometheus。
3. 演示时能证明数据来自真实 Linux 系统，而不是页面模拟。

## 2026-05-12 阶段三：真实 Linux 运维节点接入

### 1. WSL Ubuntu 准备

安装并使用 WSL2 发行版：

```text
Ubuntu-24.04
```

节点信息：

```text
hostname: heyan
system: Ubuntu 24.04.4 LTS
kernel: 6.6.87.2-microsoft-standard-WSL2
```

### 2. node_exporter 安装

在 WSL 内安装：

```text
node_exporter v1.10.2
```

监听地址：

```text
0.0.0.0:19100
```

启动脚本：

```text
/usr/local/bin/start-node-exporter.sh
```

### 3. WSL Prometheus 安装

在 WSL 内安装：

```text
Prometheus v3.7.3
```

监听地址：

```text
0.0.0.0:19090
```

配置文件：

```text
/etc/prometheus-wsl/prometheus.yml
```

采集目标：

```text
127.0.0.1:19100
```

### 4. 问题定位与修复

启动 WSL Prometheus 时曾出现“脚本提示 already running，但实际没有 Prometheus 进程”的情况。

原因：

原脚本使用的 `pgrep -f prometheus-wsl` 匹配范围过宽，会把当前执行的启动命令本身误判为 Prometheus 进程。

修复方式：

将判断逻辑改为：

1. 使用 `pgrep -x prometheus` 获取真实 Prometheus 进程。
2. 再读取 `/proc/<pid>/cmdline`。
3. 判断命令行中是否包含 `/etc/prometheus-wsl/prometheus.yml`。

修复后验证：

```text
Prometheus Server is Ready.
up{job="wsl-ubuntu-node"} = 1
node_uname_info{nodename="heyan", sysname="Linux"} = 1
```

### 5. Windows 侧脚本封装

为了后续演示和复现，新增脚本：

```text
scripts/start-wsl-real-node.ps1
scripts/check-wsl-real-node.ps1
```

作用：

1. 启动 WSL 内 node_exporter。
2. 启动 WSL 内 Prometheus。
3. 自动显示 WSL IP。
4. 验证 Windows 是否能访问 WSL Prometheus。

## 2026-05-12 阶段四：项目代码骨架创建

### 1. 后端骨架

创建 Spring Boot 后端模块：

```text
backend
```

已实现接口：

```http
GET /api/health
GET /api/monitor/prometheus/query
GET /api/monitor/prometheus/targets
GET /api/monitor/prometheus/sources
```

后端支持两个 Prometheus 数据源：

| source | 说明 |
|---|---|
| `docker` | Docker Prometheus，地址 `http://127.0.0.1:9090` |
| `wsl` | WSL Prometheus，地址 `http://<WSL-IP>:19090` |

### 2. 后端问题修复

联调时 Prometheus 查询接口返回 `500`。

日志显示：

```text
Name for argument of type [java.lang.String] not specified,
and parameter name information not available via reflection.
```

原因：

Spring Boot 在当前编译参数下不能通过反射拿到 `@RequestParam` 的方法参数名。

修复方式：

显式指定参数名：

```java
@RequestParam(name = "source", defaultValue = "docker")
@RequestParam(name = "query", defaultValue = "up")
```

修复后验证：

```text
GET /api/monitor/prometheus/query?source=docker&query=up 成功
GET /api/monitor/prometheus/query?source=wsl&query=up 成功
GET /api/monitor/prometheus/query?source=wsl&query=node_uname_info 成功
```

### 3. 前端骨架

创建 Vue 3 前端模块：

```text
frontend
```

当前页面已展示：

1. 后端服务状态。
2. Docker 观测目标数量和状态。
3. WSL Ubuntu 真实 Linux 节点状态。
4. Linux 主机名、系统、内核信息。
5. 最近一次 Prometheus 原始响应。

验证结果：

```text
npm run build 成功
前端开发服务 http://127.0.0.1:5173 可访问
```

### 4. AI 服务骨架

创建 FastAPI 服务模块：

```text
ai-service
```

当前主要用于后续接入大模型 API、RAG 知识库和故障诊断接口。

## 2026-05-12 阶段五：仓库连接与过程文档补充

### 1. Git 仓库初始化

在项目源码目录初始化 Git：

```text
E:\.AA毕业设计\10_项目源码
```

远程仓库：

```text
https://github.com/wrj800/-itc-.git
```

首个提交：

```text
c67d937 first commit
```

### 2. 推送过程问题

首次提交失败，原因是本地 Git 没有配置提交身份：

```text
Author identity unknown
```

处理方式：

只为当前仓库设置本地身份，不修改全局 Git 配置：

```powershell
git config user.name "wrj800"
git config user.email "wrj800@users.noreply.github.com"
```

之后提交并推送成功。

### 3. 文档体系建立

新增并维护以下文档：

```text
docs/开发文档.md
docs/开发记录.md
docs/开发过程记录.md
docs/环境启动说明.md
docs/环境配置完成记录.md
deploy/wsl-real-node.md
```

后续约定：

1. `开发文档.md` 记录架构、模块、接口和规范。
2. `开发记录.md` 记录每次开发变更和验证结果。
3. `开发过程记录.md` 记录阶段推进、问题定位和解决过程。

## 当前阶段总结

截至 2026-05-12，项目已经完成从“选题设想”到“可运行骨架”的第一阶段闭环。

已经具备：

1. 可运行的前端。
2. 可运行的 Spring Boot 后端。
3. 可运行的 FastAPI AI 服务骨架。
4. 可运行的 Docker 观测栈。
5. 可运行的 WSL 真实 Linux 节点采集链路。
6. GitHub 远程仓库。
7. 持续维护的开发文档、开发记录和开发过程记录。

下一阶段重点：

1. 设计并实现设备资产管理。
2. 设计并实现监控目标管理。
3. 将 Prometheus 查询结果沉淀为业务对象。
4. 实现告警规则和告警列表。
5. 为 AI 故障诊断准备真实指标和告警上下文。

## 2026-05-12 阶段六：系统原型设计

### 1. 原型设计思路

在完成基础环境和真实节点接入后，我开始进行系统原型设计。本阶段按照“设计先行”的方式推进，先完成设计图、流程图和 UML，再进入页面实现。

设计重点：

1. 保留真实监控数据入口。
2. 明确设备、监控、告警、工单、AI 诊断之间的关系。
3. 页面结构尽量贴近企业运维系统，而不是普通课程演示页面。
4. 为后续数据库表、接口和业务流程提供依据。

### 2. 原型模块

当前前端原型包含六个模块：

1. 运维总览。
2. 设备资产。
3. 监控目标。
4. 告警中心。
5. 工单流转。
6. AI 诊断。

### 3. 设计产物

本阶段补充了以下正式设计材料：

1. `03_系统设计/原型与流程设计说明书.md`。
2. `03_系统设计/页面线框图.md`。
3. `03_系统设计/UML/10_原型页面导航图.puml`。
4. `03_系统设计/UML/11_监控采集活动图.puml`。
5. `03_系统设计/UML/12_AI故障诊断活动图.puml`。
6. `03_系统设计/UML/13_ER图.puml`。

### 4. 页面设计

页面设计重点：

1. 左侧导航支持模块切换。
2. 运维总览展示真实 Linux 节点状态。
3. 设备资产页提供搜索和状态筛选。
4. 监控目标页保留 PromQL 查询调试。
5. 告警中心提供告警等级筛选和转工单入口。
6. 工单流转使用看板式布局。
7. AI 诊断页预留大模型分析入口。

### 5. 原型价值

该原型可以用于：

1. 开题或中期检查时展示系统雏形。
2. 与数据库设计、接口设计进行对应。
3. 后续按模块逐步替换静态数据。
4. 证明本项目不是单纯页面模拟，而是围绕真实监控链路设计。

### 6. UML 工具化导出

为了让设计材料更适合论文和答辩使用，我使用 PlantUML + Graphviz 对 UML 源文件进行专业渲染。

本阶段完成：

1. 安装 Graphviz。
2. 下载 PlantUML。
3. 配置 `GRAPHVIZ_DOT`。
4. 将 13 个 `.puml` 文件批量导出为 PNG。
5. 将 13 个 `.puml` 文件批量导出为 SVG。
6. 将导出结果同步到源码仓库。

导出后，UML 图既保留可维护的 `.puml` 源文件，也保留可直接插入论文的图片文件。

## 2026-05-12 阶段七：设计图审查与规范化

### 1. 审查目标

我对当前设计图进行了一次完整性和专业性检查，重点关注：

1. 是否覆盖毕业设计常见必要图。
2. 是否存在新旧版本文档并存。
3. UML 图是否能通过专业工具渲染。
4. 图的内容是否与当前真实环境一致。
5. 导出图是否与源文件同步。

### 2. 审查结论

当前设计图已经覆盖：

1. 用例图。
2. 类图。
3. ER 图。
4. 组件图。
5. 部署图。
6. 时序图。
7. 活动图。
8. 状态图。
9. 页面导航图。
10. 页面线框图。
11. 虚拟实验拓扑图。

### 3. 本次优化

本次优化内容：

1. 删除重复的 `docs/原型设计说明.md`，原型设计说明统一维护到 `docs/设计图/原型与流程设计说明书.md`。
2. 用例图改为总览级图，减少连线交叉，突出核心参与者和系统能力。
3. 核心类图按权限域、资产与采集域、告警与工单域、智能诊断域分包。
4. 告警转工单时序图补充 Controller、Service、数据库、通知待办边界。
5. 组件图按前端展示层、Spring Boot 业务层、监控采集层、AI 服务层分层。
6. 部署图调整为当前真实环境，体现 Windows、Docker Desktop、WSL2 Ubuntu、AI 服务和后续数据层。
7. ER 图补充关联表、主外键和日志分析记录。
8. 新增告警状态图和工单状态图。
9. 每次导出前清空旧 PNG/SVG，避免导出图新旧版本共存。

### 4. 当前唯一最新版

当前设计源文件统一维护在：

```text
docs/设计图/UML
```

当前设计图片统一维护在：

```text
docs/设计图/导出图/PNG
docs/设计图/导出图/SVG
```

过程变化只记录在本文档中，不再额外保留旧版设计文件。

## 2026-05-12 阶段八：开发规则与 AI 接手规范建立

### 1. 建立原因

随着项目逐步进入设计和开发阶段，我需要一份单独的规则文档，保证后续无论是我继续开发，还是由 AI 或其他开发者接手，都能遵循同一套规则，避免出现以下问题：

1. 文档新旧版本并存。
2. 设计图导出文件和源文件不同步。
3. 代码改了但开发记录没有更新。
4. 环境配置变了但启动说明没有更新。
5. AI 接手时不知道应该先读哪些文件。

### 2. 本阶段处理

本阶段新增：

```text
docs/开发规则与AI接手规范.md
```

该文档明确：

1. 开发最高原则。
2. AI 接手前必须阅读的文件。
3. 文档维护规则。
4. 开发记录规则。
5. 开发过程记录规则。
6. 设计图维护规则。
7. 代码开发规则。
8. 验证规则。
9. Git 提交规则。
10. 接手检查清单。

### 3. 关键规范

最重要的规范是：

1. 除 `docs/开发过程记录.md` 外，不保留旧版设计、旧版接口、旧版配置文件。
2. 每次功能开发都必须同步更新 `docs/开发记录.md`。
3. 方案变化、问题定位和历史过程只写入 `docs/开发过程记录.md`。
4. 修改 UML 后必须清空旧导出图并重新生成 PNG/SVG。
5. 每次阶段完成后必须提交并推送 GitHub。

### 4. 补充完成后复盘要求

我进一步补充了“每次完成后必须复盘并建议下一步”的规则。这样做的目的是让每次阶段结束都能形成闭环，不只停留在“完成当前任务”，还要检查是否有遗漏，并明确下一步应该推进什么。

后续每次阶段总结都需要包含：

1. 已完成。
2. 验证结果。
3. 风险或遗留问题。
4. 建议下一步。

## 2026-05-12 阶段九：第一版运维业务闭环开发

### 1. 开发目标

本阶段开始从“设计图和环境准备”进入“可运行业务原型”开发。我希望先打通一条能演示的运维闭环，而不是只停留在静态页面：

1. 设备资产可以被前端读取和展示。
2. 监控目标可以从业务接口展示，同时保留真实 Prometheus 查询入口。
3. 告警可以转成工单。
4. 工单可以在看板中展示。
5. AI 诊断入口可以根据故障描述返回初步诊断结果。

### 2. 后端实现过程

后端新增了 `OpsController`，当前先使用内存数据模拟业务对象，原因是：

1. 可以尽快验证页面、接口和流程是否正确。
2. 字段结构与 ER 图保持接近，后续迁移到 MySQL 时改动范围可控。
3. 在数据库持久化前，先验证“告警转工单”和“AI 诊断”这两个核心交互。

本阶段后端接口包括：

```text
GET  /api/ops/overview
GET  /api/ops/assets
GET  /api/ops/targets
GET  /api/ops/alerts
GET  /api/ops/work-orders
POST /api/ops/alerts/{alertId}/work-orders
POST /api/ops/diagnose
```

同时新增 `GlobalExceptionHandler`，统一处理非法 JSON、参数校验和业务参数错误。

开发中发现 `@PathVariable` 如果不显式写参数名，在当前 Maven 编译参数下会出现：

```text
Name for argument of type [java.lang.String] not specified, and parameter name information not available via reflection.
```

处理方式是将方法参数改为：

```java
@PathVariable("alertId") String alertId
```

这样不依赖编译器是否保留参数名，更适合后续稳定运行。

### 3. 前端实现过程

前端将原来的简单状态页重写为单页运维工作台，保留专业运维系统常见的信息密度和布局方式：

1. 左侧模块导航。
2. 顶部状态和刷新按钮。
3. 运维总览指标卡片。
4. 实验链路和真实 Linux 节点状态。
5. 设备资产表格。
6. 监控目标和 PromQL 查询。
7. 告警列表和转工单按钮。
8. 工单看板。
9. AI 故障诊断输入和结果展示。

本阶段没有增加前端路由，原因是当前毕业设计演示以单页闭环为主，先保证交互完整和运行稳定。后续模块复杂后，再考虑拆分组件和路由。

### 4. 验证过程

本阶段验证分为构建验证和接口验证。

构建验证：

```text
. .\set-env.ps1
mvn -q -f E:\.AA毕业设计\10_项目源码\pom.xml test
npm run build
```

验证结果：

1. Maven 测试通过。
2. Vite 构建通过。
3. Vite 提示部分构建产物超过 500 kB，这是依赖体积提示，不是构建失败。

接口验证：

```text
GET  http://127.0.0.1:8080/api/health
GET  http://127.0.0.1:8080/api/ops/overview
POST http://127.0.0.1:8080/api/ops/diagnose
POST http://127.0.0.1:8080/api/ops/alerts/ALT-20260512-001/work-orders
```

验证结果：

1. `/api/health` 返回 200。
2. `/api/ops/overview` 返回 200。
3. `/api/ops/diagnose` 返回 200。
4. `/api/ops/alerts/{alertId}/work-orders` 返回 200。
5. 非法 JSON 请求返回 400，message 为“请求体不是合法 JSON”。

### 5. 过程问题

本阶段遇到两个容易误判的问题：

1. 直接执行 `mvn` 时当前 PowerShell 会话中没有 Maven PATH，命令没有启动。使用 `set-env.ps1` 或 Maven 完整路径后验证通过。
2. 在 PowerShell 中用 `curl.exe` 手写 JSON 时，反斜杠转义不是 bash 语义，导致后端收到非法 JSON。改用 PowerShell 原生对象 `ConvertTo-Json` 后，请求可以正确执行。

这两个问题都不是业务代码失败，后续排查时需要区分“命令没有启动”“请求体构造错误”和“接口业务失败”。

### 6. 当前结论

目前项目已经具备第一版可演示闭环：

1. 有真实运维采集入口。
2. 有业务化的资产、监控、告警、工单和诊断接口。
3. 有可运行的前端运维工作台。
4. 有构建和接口验证记录。

下一阶段应进入数据库持久化，先把当前内存对象迁移到 MySQL 表和初始化数据，再继续做完整 CRUD。

## 2026-05-12 阶段十：前端视觉升级与角色入口分离

### 1. 调整原因

在第一版前端工作台完成后，我发现页面虽然具备业务闭环，但视觉质感还不够接近真实企业运维平台。后续参考了新的高保真界面方向，重点改进：

1. 页面更像企业级 ICT 运维后台，而不是普通演示页。
2. 管理员和维护人员职责分离。
3. 登录页需要区分管理员入口和维护人员入口。
4. 维护人员不应该看到系统设置、用户权限、告警规则等管理员功能。

### 2. 本阶段前端设计

本阶段将 `frontend/src/App.vue` 改为角色分离式前端：

1. 未登录时显示登录分流页。
2. 选择管理员入口后进入管理员后台。
3. 选择维护人员入口后进入维护人员工作台。
4. 管理员端使用完整后台导航。
5. 维护人员端使用精简导航，只聚焦处置工作。

管理员端导航：

```text
总览
监控中心
资源管理
告警中心
工单中心
自动化巡检
报表分析
系统设置
```

维护人员端导航：

```text
我的工单
告警待办
设备状态
处置记录
```

### 3. 视觉稿导出

为了让后续开发和答辩材料有明确视觉依据，本阶段使用本地前端页面导出了 3 张 PNG 视觉稿：

```text
docs/设计图/前端视觉稿/01_登录分流页.png
docs/设计图/前端视觉稿/02_管理员后台.png
docs/设计图/前端视觉稿/03_维护人员工作台.png
```

截图方式：

```text
Edge headless
窗口尺寸：1600x900
访问地址：http://127.0.0.1:5173
```

### 4. 验证与修正

本阶段执行：

```text
npm run build
```

验证结果：

1. 前端构建通过。
2. 登录分流页可显示管理员入口和维护人员入口。
3. 管理员后台可展示总览、指标、表格、告警和待处理工单。
4. 维护人员工作台可展示我的工单、处置面板和 AI 辅助诊断入口。

截图检查时发现维护人员 KPI 卡片曾出现文字竖排、首屏密度不均衡问题，随后调整了 KPI 卡片网格、页面密度和看板布局。

### 5. 当前结论

当前前端视觉方向已经更接近目标参考图，角色边界也更清晰。下一步需要将当前前端演示态改为真实登录态：

1. 后端增加用户、角色、权限接口。
2. 前端增加路由和路由守卫。
3. 登录后根据角色进入管理员后台或维护人员工作台。
4. 将角色权限与工单责任人、告警处理人关联。

## 2026-05-12 阶段十一：前端视觉循环自检与专业化修正

### 1. 自检原因

在完成管理员端与维护人员端分离后，我继续从截图角度检查页面，发现仍然存在一些会影响答辩展示和真实业务可信度的问题：

1. 登录页虽然完成了角色入口分离，但中间区域更像装饰背景，真实运维拓扑感不足。
2. 管理员后台顶部日期范围被截断，告警级别图例在窄侧栏中出现单字换行。
3. 管理员后台部分 KPI 数据过于演示化，不像真实纳管规模。
4. 维护人员工作台 KPI 曾出现文案竖排和 `8分钟` 换行问题。
5. 维护人员右侧详情一开始更像空表单，不像真实待处理工单。
6. 维护人员看板在 4 列展示时曾被挤到下一行，页面显得不够专业。

### 2. 本阶段修正

本阶段没有新增旧版本文件，而是在现有页面和视觉稿上直接更新：

1. 登录页增加运维拓扑节点，展示 Prometheus、Redis、Linux VM、node_exporter 和核心设备关系。
2. 管理员端修正日期范围宽度，统一使用 `2026-05-12 00:00 ~ 2026-05-12 23:59`。
3. 管理员端将 KPI 指标调整为在线设备 `116`、活跃告警 `9`、今日工单 `23` 等更符合生产运维语境的数据。
4. 管理员端收紧告警分布图尺寸和图例布局，避免图例文字纵向断裂。
5. 维护端将右侧面板改为工单详情，补充 `WO-20260512-1001`、关联告警、SLA、故障描述、AI 诊断建议、处理按钮和处理记录。
6. 维护端修正预览登录身份，管理员显示 `张伟`，维护人员显示 `李四`。
7. 维护端重新组织左侧 KPI 与工单看板结构，避免右侧详情面板把左侧网格行高撑开。

### 3. 循环验证

本阶段多次执行：

```text
npm run build
```

验证结果：

1. 前端构建通过。
2. Vite 仍提示部分 chunk 超过 500 kB，这是依赖和打包体积提示，不是失败。
3. `@vueuse/core` 的 `#__PURE__` 注释提示由 Rollup 输出，不影响页面运行。

截图验证：

```text
http://127.0.0.1:5173
http://127.0.0.1:5173?role=admin&view=overview
http://127.0.0.1:5173?role=maintainer&view=myTickets
```

重新导出的视觉稿：

```text
docs/设计图/前端视觉稿/01_登录分流页.png
docs/设计图/前端视觉稿/02_管理员后台.png
docs/设计图/前端视觉稿/03_维护人员工作台.png
```

### 4. 当前结论

当前 3 张核心视觉稿已经比上一版更适合毕业设计展示：

1. 登录页能体现真实运维环境，而不是单纯登录表单。
2. 管理员端职责聚焦统一管理、监控、告警、工单和报表。
3. 维护人员端职责聚焦接单、处置、AI 辅助诊断和处理记录。
4. 页面中没有继续保留新旧版本共存文件，视觉稿使用原路径实时覆盖更新。

### 5. 下一步建议

下一步建议进入前端工程化拆分和真实权限阶段：

1. 将 `App.vue` 中的管理员页面、维护人员页面、通用卡片、表格、看板拆为独立组件。
2. 引入 Vue Router，建立管理员端和维护人员端独立路由。
3. 后端新增用户、角色、权限、登录接口。
4. 前端增加登录状态、角色守卫和菜单权限。
5. 工单按钮接入真实接口，包括接单、转派、提交处置、关闭工单。

## 2026-05-12 阶段十二：优秀开源项目对标与工程化补强

### 1. 调整原因

前端视觉稿完成后，我继续思考“如何把项目做得更像优秀项目”，不能只停留在页面好看。优秀毕业设计和优秀开源项目的差别，主要体现在：

1. 项目是否能稳定运行。
2. 文档是否能让别人接手。
3. 代码结构是否能继续扩展。
4. 是否有测试和自动化验证。
5. 是否有规范的 Issue、PR、贡献流程。
6. 是否能把技术亮点讲清楚，而不是单纯堆技术名词。

因此本阶段先补项目工程化规范，再继续做功能开发。

### 2. 对标结论

本阶段参考的优秀项目方向：

1. Spring PetClinic：学习小而完整的 Spring Boot 项目结构、领域模型和测试思路。
2. JHipster：学习前后端分离、权限、Docker、CI、OpenAPI、数据库迁移等工程完整度。
3. Vue Vben Admin / SoybeanAdmin：学习 Vue 3 后台管理系统的路由、权限、组件化和界面组织。
4. Prometheus 官方实践：学习监控目标、告警规则和可观测性闭环。
5. GitHub Open Source Guides：学习 README、贡献说明、Issue 模板、PR 模板和协作规范。

我将这些经验整理到了：

```text
docs/优秀项目对标与改进方案.md
```

### 3. 本阶段新增工程化文件

新增文件：

```text
CONTRIBUTING.md
.github/workflows/ci.yml
.github/ISSUE_TEMPLATE/bug_report.md
.github/ISSUE_TEMPLATE/feature_request.md
.github/pull_request_template.md
docs/优秀项目对标与改进方案.md
```

这些文件的作用：

1. `CONTRIBUTING.md`：说明开发顺序、提交规范、验证要求和不接受的改动。
2. `ci.yml`：在 GitHub 上自动运行后端 Maven 测试和前端构建。
3. Issue 模板：规范 Bug 和功能需求的描述方式。
4. PR 模板：强制检查验证、文档同步和安全风险。
5. 对标方案：作为后续“如何做成优秀项目”的路线图。

### 4. 文档同步

本阶段同步更新：

```text
README.md
docs/开发规则与AI接手规范.md
docs/开发文档.md
docs/开发记录.md
docs/开发过程记录.md
```

其中 `docs/开发规则与AI接手规范.md` 增加了“优秀项目工程化规则”，要求 CI、Issue 模板、PR 模板和贡献说明作为长期规范保留。

### 5. 当前结论

当前项目已经从“能演示的毕业设计项目”向“有工程化规范的项目”前进了一步。下一步不能继续只写文档，应开始落实对标方案中的代码结构改进。

最优先的改进是：

1. 拆分 `App.vue`。
2. 引入 Vue Router。
3. 建立管理员端和维护人员端独立页面目录。
4. 把通用 KPI、表格、看板和详情面板抽为组件。
5. 保留当前视觉效果不退化。

### 6. 下一步建议

下一步建议进入“前端工程化拆分”阶段。原因是当前所有主要页面仍集中在 `App.vue`，如果马上继续写登录、权限、数据库联调，文件会越来越难维护。先拆分结构，再做真实权限和持久化，是更稳的路线。

## 2026-05-13 阶段十三：大型运维平台 UI 规范与循环优化

### 1. 调整原因

继续检查前端后，我发现项目需要的不只是“更好看”，而是要有大型运维平台的组件规则。运维系统和普通管理后台不同，核心是快速识别状态、定位风险、执行处置，所以 UI 组件必须服务于真实运维流程。

本阶段重点从以下角度改进：

1. 管理员端要像 NOC 指挥台，先看采集状态、告警管道、SLA 风险和同步状态。
2. 维护人员端要像值班工作台，先看班组、负责范围、交接记录和升级通道。
3. 工单看板不能因为后端当前无数据就大面积空白，必须有空状态或演示待办。
4. UI 规范需要沉淀成文档，后续每个组件都按规则实现。

### 2. 新增 UI 规范

新增：

```text
docs/运维平台UI设计规范.md
```

该文档定义了：

1. 总体设计原则。
2. 管理员后台布局规则。
3. 维护人员工作台布局规则。
4. KPI、状态条、表格、告警、工单看板、工单详情、AI 诊断组件规则。
5. 视觉规则、交互规则和截图验收规则。

### 3. 页面优化

管理员端：

1. 新增指挥状态条。
2. 展示采集状态、告警管道、SLA 风险和最近同步。
3. 让首屏更像运维态势总览，而不是普通 KPI 堆叠。

维护人员端：

1. 新增班组值班状态条。
2. 展示当前班组、负责范围、交接状态、升级通道。
3. 工单看板增加演示待办和空状态，避免无数据时页面显得失效。

### 4. 循环验证

执行：

```text
npm run build
```

结果：

1. 前端构建通过。
2. Vite 仍提示 chunk 体积和 `@vueuse/core` 注释问题，这是构建警告，不是失败。

截图过程：

1. 第一次截图时发现三张截图文件异常偏小。
2. 检查发现 `http://127.0.0.1:5173` 无法访问，原因是前端开发服务未运行。
3. 重新启动 Vite 后，页面返回 200。
4. 重新导出管理员后台和维护人员工作台截图。
5. 人工检查确认新增状态条、值班条、空状态和演示待办显示正常。

### 5. 当前结论

当前 UI 比上一版更接近大型运维平台：

1. 管理员端有更明确的运行态势入口。
2. 维护人员端有更明确的值班语境。
3. 工单看板在无真实数据时也有合理展示。
4. 后续 UI 开发有了可执行规范。

### 6. 下一步建议

下一步建议不要继续在 `App.vue` 里堆 UI，而是进入组件化拆分：

1. 拆出 `components/common/MetricTile.vue`。
2. 拆出 `components/common/CommandBar.vue`。
3. 拆出 `components/work-order/KanbanColumn.vue`。
4. 拆出 `components/work-order/TicketDetail.vue`。
5. 拆出 `pages/admin/AdminDashboard.vue`。
6. 拆出 `pages/maintainer/MaintainerDesk.vue`。
7. 引入 Vue Router，为真实登录和权限守卫做准备。

## 2026-05-13 阶段十四：登录页双角色表单对齐修正

### 1. 调整原因

检查登录页后，我发现管理员入口和维护人员入口虽然已经分离，但表单结构不一致：维护人员多了“班组”字段，管理员没有对应字段，导致切换角色时卡片内容高度变化。普通 `label` 样式还影响了复选框行，容易造成账号密码输入框、复选框和入口切换按钮的尺寸不统一。

本阶段我重点修正以下问题：

1. 两种登录界面不对齐。
2. 账号和密码输入组件视觉上不稳定。
3. 切换角色时表单区域上下跳动。
4. 字段文字、按钮文字和选项文字缺少统一换行规则。
5. 短文本能一列显示却被布局拆散。

### 2. 代码调整

在 `frontend/src/App.vue` 中，我把登录表单统一为固定三行：

```text
账号/工号
登录密码
管理范围/所属班组
```

管理员第三行显示“管理范围”，维护人员第三行显示“所属班组”。两种角色使用同一套字段数量和同一套控件结构，只替换字段文案和选项内容。

同时新增：

```text
setLoginRole(role)
loginScopeOptions
?loginRole=admin
?loginRole=maintainer
```

这样后续可以直接通过 URL 预览两种登录态，不需要手动点击后再截图。

### 3. 样式调整

在 `frontend/src/style.css` 中，我把登录表单样式拆成独立类：

```text
.login-form-grid
.login-field
.login-field-label
```

并固定了：

1. 登录卡片结构：标题区、表单区、选项区、提交按钮区。
2. 输入框和选择框高度：`52px`。
3. 字段标签高度和单行显示。
4. 复选框行独立样式，不再受普通字段布局影响。
5. 切换按钮、字段标签、选项文字和入口按钮默认不换行。

### 4. 文档同步

我更新了：

```text
docs/运维平台UI设计规范.md
docs/开发文档.md
docs/设计图索引.md
docs/开发记录.md
docs/开发过程记录.md
```

其中 UI 规范新增“登录入口与表单组件”规则，明确后续不允许管理员入口和维护人员入口出现字段数量不同、控件高度不同或切换角色上下跳动的问题。

### 5. 验证过程

执行前端构建：

```text
npm run build
```

结果：

1. 构建通过。
2. Vite 仍提示 chunk 体积超过 500 kB。
3. `@vueuse/core` 仍有注释移除警告。
4. 这些是既有构建警告，不是失败。

检查前端服务：

```text
http://127.0.0.1:5173
```

结果返回 `200`。

浏览器验收：

```text
http://127.0.0.1:5173?loginRole=admin
http://127.0.0.1:5173?loginRole=maintainer
```

检查结果：

1. 管理员登录显示“管理员账号、登录密码、管理范围”。
2. 维护人员登录显示“维护人员工号、登录密码、所属班组”。
3. 两种角色登录卡片高度一致。
4. 输入框、选择框、复选框行和主按钮位置一致。
5. 未出现字段文字被拆成多列的问题。

视觉稿覆盖更新：

```text
docs/设计图/前端视觉稿/01_登录分流页.png
```

截图尺寸为 1600x900，文件大小为 369902 bytes。

### 6. 当前结论

登录页双角色入口已经从“两个入口各自布局”调整为“同一套表单组件体系”。这样后续接真实登录、权限和菜单守卫时，不会因为管理员和维护人员字段差异导致 UI 抖动。

### 7. 下一步建议

下一步建议进入前端组件化拆分，优先拆：

1. `LoginView.vue`
2. `AdminDashboard.vue`
3. `MaintainerDesk.vue`
4. `MetricTile.vue`
5. `CommandBar.vue`
6. `KanbanColumn.vue`

拆分后再引入 Vue Router 和真实登录权限，会比继续在 `App.vue` 里堆功能更稳。

## 2026-05-13 阶段十五：登录页组件拆分与顶部按钮回归修正

### 1. 调整原因

上一阶段我已经把登录页双角色表单对齐，但登录页结构仍然和管理员后台、维护人员工作台混在 `App.vue` 中。继续这样开发会导致两个问题：

1. 后续接真实登录、JWT、角色权限时，登录表单和工作台逻辑会互相影响。
2. `App.vue` 文件过大，后续维护人员接手时很难快速定位登录页、管理员页和维护人员页的边界。

所以本阶段我先做一个低风险拆分：只拆登录页，不大改工作台业务逻辑，确保视觉不退化。

### 2. 组件拆分

新增：

```text
frontend/src/components/LoginView.vue
```

该组件负责：

1. 管理员入口和维护人员入口切换。
2. 账号/工号、登录密码、管理范围/所属班组三行表单。
3. 登录页运维拓扑视觉。
4. `?loginRole=admin` 和 `?loginRole=maintainer` 预览参数。
5. 点击登录后向父组件抛出 `login` 事件。

`App.vue` 调整为：

```text
<LoginView v-if="!session" @login="login" />
```

主应用只接收登录角色，并决定进入管理员总览还是维护人员工作台。

### 3. 回归问题修正

拆分后我做浏览器视觉回归时发现：管理员和维护人员页面顶部工具栏里的“刷新”按钮被挤成了上下两行。这个问题不是拆分导致的类型错误，而是按钮样式缺少稳定尺寸和不换行约束。

本阶段在 `frontend/src/style.css` 中补充：

1. 通用按钮 `white-space: nowrap`。
2. 通用按钮 `line-height: 1`。
3. `outline-button` 和 `primary-button` 最小宽度。
4. 页面动作区按钮垂直居中。

修正后，“刷新”“导出报表”“创建工单”“提交处置”等按钮都保持单行显示。

### 4. 文档同步

我更新了：

```text
docs/开发文档.md
docs/开发规则与AI接手规范.md
docs/运维平台UI设计规范.md
docs/设计图索引.md
docs/开发记录.md
docs/开发过程记录.md
```

文档中明确：

1. 登录页维护入口是 `frontend/src/components/LoginView.vue`。
2. 后续不要把登录表单重新堆回 `App.vue`。
3. 工具栏和页面动作按钮必须保持单行显示。

### 5. 验证过程

执行：

```text
npm run build
```

结果：

1. 构建通过。
2. Vite chunk 体积和 `@vueuse/core` 注释提示仍存在，但不是失败。

浏览器检查：

```text
http://127.0.0.1:5173?loginRole=admin
http://127.0.0.1:5173?role=admin&view=overview
http://127.0.0.1:5173?role=maintainer&view=myTickets
```

检查结果：

1. 登录页显示正常。
2. 管理员总览显示正常。
3. 维护人员工作台显示正常。
4. 顶部“刷新”按钮不再断行。
5. 管理员“导出报表”“创建工单”和维护人员“提交处置”按钮显示正常。

视觉稿覆盖更新：

```text
docs/设计图/前端视觉稿/01_登录分流页.png
docs/设计图/前端视觉稿/02_管理员后台.png
docs/设计图/前端视觉稿/03_维护人员工作台.png
```

截图文件均正常写入。Edge headless 输出 fallback task provider 日志，但截图成功，不属于页面运行失败。

### 6. 当前结论

登录页已经从 `App.vue` 中拆出，登录部分的职责更清晰；同时通过回归测试修复了顶部工具栏按钮断行问题。当前项目已经开始从“单文件前端演示”向“可维护前端结构”过渡。

### 7. 下一步建议

下一步建议继续拆分登录后的工作台区域：

1. `AdminDashboard.vue`
2. `MaintainerDesk.vue`
3. `MetricTile.vue`
4. `CommandBar.vue`
5. `KanbanColumn.vue`
6. `TicketDetail.vue`

完成这些拆分后，再引入 Vue Router 和真实权限守卫，会更稳。
