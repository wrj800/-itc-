# 环境配置完成记录

完成时间：2026-05-12

## 一、JDK17 与 Maven

已将 Maven 复制到纯英文路径，避免中文路径在 PowerShell 中乱码：

```text
E:\Acodex\tools\apache-maven-3.9.9
```

项目脚本：

```text
E:\.AA毕业设计\10_项目源码\set-env.ps1
```

验证结果：

```text
Apache Maven 3.9.9
Java version: 17.0.18
BUILD SUCCESS
```

## 二、Docker 观测栈

已拉取镜像：

```text
redis:8-alpine
prom/prometheus:v3.7.3
prom/blackbox-exporter:v0.27.0
prom/node-exporter:v1.10.2
```

已启动容器：

```text
ict-redis
ict-prometheus
ict-blackbox-exporter
ict-node-exporter
```

验证结果：

```text
Redis: PONG
Prometheus: Ready
Prometheus query up: success
node-exporter: up=1
blackbox-http: up=1
prometheus: up=1
```

## 三、代码骨架

已创建：

```text
backend       Spring Boot
frontend      Vue 3 + Vite + Element Plus
ai-service    FastAPI
deploy        Docker Compose + Prometheus 配置
scripts       启动脚本
docs          运行说明
```

## 四、已验证事项

| 项目 | 结果 |
|---|---|
| Docker daemon | 可用 |
| Docker Compose | 可用 |
| Redis 容器 | 可用 |
| Prometheus 容器 | 可用 |
| Blackbox Exporter 容器 | 可用 |
| node_exporter 测试节点 | 可被 Prometheus 采集 |
| Spring Boot 后端构建 | 成功 |
| Vue 前端依赖安装 | 成功 |
| FastAPI 虚拟环境安装 | 成功 |
| WSL Ubuntu 真实 Linux 节点 | 成功 |
| WSL node_exporter | `http://172.18.219.55:19100/metrics` 可访问 |
| WSL Prometheus | `http://172.18.219.55:19090` 可查询 |
| 真实 Linux 节点采集 | `up{job="wsl-ubuntu-node"}=1` |
| Linux 主机信息指标 | `node_uname_info{nodename="heyan", sysname="Linux"}=1` |

## 五、遗留事项

1. 后续需要实现真实业务模块：设备、采集目标、告警、工单、知识库。
2. 后续需要补充数据库连接和实体表映射。
3. 后续需要接入 AI 服务，完成告警摘要、根因分析和处置建议。
4. 后续需要把前后端页面继续扩展成完整毕业设计系统。
