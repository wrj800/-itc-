# 开发记录

## 2026-05-12

### 初始化项目仓库

完成内容：

1. 在 `E:\.AA毕业设计\10_项目源码` 初始化 Git 仓库。
2. 添加远程仓库 `https://github.com/wrj800/-itc-.git`。
3. 创建首个提交 `c67d937 first commit`。
4. 推送 `main` 分支到 GitHub。
5. 增加 `.gitignore`，排除生成物和本地环境文件。

验证结果：

```text
git status: main...origin/main
远程仓库推送成功
```

### 完成基础运行环境

完成内容：

1. 固定 `JDK17` 与 `Maven 3.9.9`。
2. Maven 放置在英文路径 `E:\Acodex\tools\apache-maven-3.9.9`。
3. 配置 `set-env.ps1` 统一设置 `JAVA_HOME` 与 `MAVEN_HOME`。
4. Docker Desktop 可用。
5. Redis、Prometheus、Blackbox Exporter、Docker node_exporter 已启动。

验证结果：

```text
Redis: PONG
Docker Prometheus: Ready
Docker node_exporter: up=1
Blackbox Exporter: 可访问
```

### 接入真实 Linux 运维节点

完成内容：

1. 安装 WSL2 Ubuntu 24.04。
2. 在 WSL 内安装 `node_exporter v1.10.2`。
3. 在 WSL 内安装 `Prometheus v3.7.3`。
4. 新增 `scripts/start-wsl-real-node.ps1`。
5. 新增 `scripts/check-wsl-real-node.ps1`。
6. 新增 `deploy/wsl-real-node.md`。

验证结果：

```text
WSL IP: 172.18.219.55
node_exporter: http://172.18.219.55:19100/metrics
WSL Prometheus: http://172.18.219.55:19090
up{job="wsl-ubuntu-node"} = 1
node_uname_info{nodename="heyan", sysname="Linux"} = 1
```

说明：

Docker Prometheus 直连 WSL 目标存在网络边界问题，因此采用 WSL 内 Prometheus 本地采集真实 Linux 节点，后端通过 `source=wsl` 查询该数据源。

### 完成代码骨架

完成内容：

1. 创建 Spring Boot 后端骨架。
2. 创建 Vue 3 前端骨架。
3. 创建 FastAPI AI 服务骨架。
4. 后端新增 Prometheus 多数据源查询接口。
5. 前端新增 Docker 观测栈与 WSL 真实 Linux 节点状态展示。

验证结果：

```text
Spring Boot /api/health = UP
GET /api/monitor/prometheus/query?source=docker&query=up 成功
GET /api/monitor/prometheus/query?source=wsl&query=up 成功
Maven build = BUILD SUCCESS
Vite build = 成功
```

### 新增持续文档维护机制

完成内容：

1. 新增 `docs/开发文档.md`。
2. 新增 `docs/开发记录.md`。
3. 新增 `docs/开发过程记录.md`。
4. 约定后续每次开发都同步更新项目文档、开发记录和开发过程记录。

后续要求：

每次新增功能、修改接口、调整环境或修复问题时，需要同时记录：

1. 修改内容。
2. 涉及文件。
3. 验证命令。
4. 验证结果。
5. 问题定位过程。
6. 遗留问题。

### 开始原型设计

完成内容：

1. 新增 `docs/原型设计说明.md`。
2. 明确原型设计阶段先完成设计图、流程图和 UML，再继续实现页面。
3. 原型设计覆盖运维总览、设备资产、监控目标、告警中心、工单流转、AI 诊断六个模块。
4. 设计中保留真实 Prometheus 与 WSL Linux 节点数据展示链路。
5. 新增 `03_系统设计/原型与流程设计说明书.md`。
6. 新增 `03_系统设计/页面线框图.md`。
7. 新增原型页面导航图、监控采集活动图、AI 故障诊断活动图、ER 图。
8. 将设计图同步到 `10_项目源码/docs/设计图/`，便于 GitHub 仓库查看。

涉及文件：

```text
03_系统设计/原型与流程设计说明书.md
03_系统设计/页面线框图.md
03_系统设计/UML/10_原型页面导航图.puml
03_系统设计/UML/11_监控采集活动图.puml
03_系统设计/UML/12_AI故障诊断活动图.puml
03_系统设计/UML/13_ER图.puml
docs/设计图/
docs/原型设计说明.md
docs/设计图索引.md
docs/开发记录.md
docs/开发过程记录.md
README.md
```

验证方式：

```text
检查设计文档和 PlantUML 文件是否已生成。
确认 docs/设计图/ 中包含页面线框图、流程设计说明书和 UML 文件。
```

遗留问题：

1. 后续需要根据设计图继续细化前端页面。
2. 后续需要将设备、告警、工单等设计转为 Spring Boot 接口和数据库表。
3. 后续需要增加趋势图、详情弹窗和表单交互。
