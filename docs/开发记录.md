# 开发记录

## 2026-05-12

### 初始化项目仓库

完成内容：

1. 在 `E:\.AA毕业设计\10_项目源码` 初始化 Git 仓库。
2. 添加远程仓库 `https://github.com/wrj800/-itc-.git`。
3. 创建首个提交 `c67d937 first commit`。
4. 推送 `main` 分支到 GitHub。
5. 增加 `.gitignore`，排除生成物和本地环境文件。

验证结果：

```text
git status: main...origin/main
远程仓库推送成功
```

### 完成基础运行环境

完成内容：

1. 固定 `JDK17` 与 `Maven 3.9.9`。
2. Maven 放置在英文路径 `E:\Acodex\tools\apache-maven-3.9.9`。
3. 配置 `set-env.ps1` 统一设置 `JAVA_HOME` 与 `MAVEN_HOME`。
4. Docker Desktop 可用。
5. Redis、Prometheus、Blackbox Exporter、Docker node_exporter 已启动。

验证结果：

```text
Redis: PONG
Docker Prometheus: Ready
Docker node_exporter: up=1
Blackbox Exporter: 可访问
```

### 接入真实 Linux 运维节点

完成内容：

1. 安装 WSL2 Ubuntu 24.04。
2. 在 WSL 内安装 `node_exporter v1.10.2`。
3. 在 WSL 内安装 `Prometheus v3.7.3`。
4. 新增 `scripts/start-wsl-real-node.ps1`。
5. 新增 `scripts/check-wsl-real-node.ps1`。
6. 新增 `deploy/wsl-real-node.md`。

验证结果：

```text
WSL IP: 172.18.219.55
node_exporter: http://172.18.219.55:19100/metrics
WSL Prometheus: http://172.18.219.55:19090
up{job="wsl-ubuntu-node"} = 1
node_uname_info{nodename="heyan", sysname="Linux"} = 1
```

说明：

Docker Prometheus 直连 WSL 目标存在网络边界问题，因此采用 WSL 内 Prometheus 本地采集真实 Linux 节点，后端通过 `source=wsl` 查询该数据源。

### 完成代码骨架

完成内容：

1. 创建 Spring Boot 后端骨架。
2. 创建 Vue 3 前端骨架。
3. 创建 FastAPI AI 服务骨架。
4. 后端新增 Prometheus 多数据源查询接口。
5. 前端新增 Docker 观测栈与 WSL 真实 Linux 节点状态展示。

验证结果：

```text
Spring Boot /api/health = UP
GET /api/monitor/prometheus/query?source=docker&query=up 成功
GET /api/monitor/prometheus/query?source=wsl&query=up 成功
Maven build = BUILD SUCCESS
Vite build = 成功
```

### 新增持续文档维护机制

完成内容：

1. 新增 `docs/开发文档.md`。
2. 新增 `docs/开发记录.md`。
3. 新增 `docs/开发过程记录.md`。
4. 约定后续每次开发都同步更新项目文档、开发记录和开发过程记录。

后续要求：

每次新增功能、修改接口、调整环境或修复问题时，需要同时记录：

1. 修改内容。
2. 涉及文件。
3. 验证命令。
4. 验证结果。
5. 问题定位过程。
6. 遗留问题。

### 开始原型设计

完成内容：

1. 统一原型设计说明，不再保留多份新旧文档。
2. 明确原型设计阶段先完成设计图、流程图和 UML，再继续实现页面。
3. 原型设计覆盖运维总览、设备资产、监控目标、告警中心、工单流转、AI 诊断六个模块。
4. 设计中保留真实 Prometheus 与 WSL Linux 节点数据展示链路。
5. 新增 `03_系统设计/原型与流程设计说明书.md`。
6. 新增 `03_系统设计/页面线框图.md`。
7. 新增原型页面导航图、监控采集活动图、AI 故障诊断活动图、ER 图。
8. 将设计图同步到 `10_项目源码/docs/设计图/`，便于 GitHub 仓库查看。

涉及文件：

```text
03_系统设计/原型与流程设计说明书.md
03_系统设计/页面线框图.md
03_系统设计/UML/10_原型页面导航图.puml
03_系统设计/UML/11_监控采集活动图.puml
03_系统设计/UML/12_AI故障诊断活动图.puml
03_系统设计/UML/13_ER图.puml
docs/设计图/
docs/设计图索引.md
docs/开发记录.md
docs/开发过程记录.md
README.md
```

验证方式：

```text
检查设计文档和 PlantUML 文件是否已生成。
确认 docs/设计图/ 中包含页面线框图、流程设计说明书和 UML 文件。
```

遗留问题：

1. 后续需要根据设计图继续细化前端页面。
2. 后续需要将设备、告警、工单等设计转为 Spring Boot 接口和数据库表。
3. 后续需要增加趋势图、详情弹窗和表单交互。

### 使用专业 UML 工具导出设计图

完成内容：

1. 安装并配置 Graphviz。
2. 下载 PlantUML 到 `E:\Acodex\tools\plantuml.jar`。
3. 使用 PlantUML + Graphviz 批量渲染 UML 图。
4. 导出 13 张 PNG 图片和 13 张 SVG 图片。
5. 新增 `docs/UML工具使用说明.md`。
6. 更新 `docs/设计图索引.md`。

优化内容：

1. 用例图按基础管理、资产采集、告警工单、智能分析分包。
2. 类图按权限域、资产与采集域、告警与工单域、智能诊断域分包。
3. 告警转工单时序图补充 Controller、Service、数据库、通知待办边界。
4. 组件图补充前端组件库、图表、后端模块、监控采集层、AI 服务层。
5. 部署图调整为当前真实环境：Windows、Docker Desktop、WSL2 Ubuntu、后续数据层和大模型服务。
6. ER 图补充关联表、主外键说明和日志分析记录。
7. 新增告警状态图和工单状态图。
8. 删除重复的 `docs/原型设计说明.md`，原型说明统一维护在 `docs/设计图/原型与流程设计说明书.md`。

输出目录：

```text
E:\.AA毕业设计\03_系统设计\UML导出\PNG
E:\.AA毕业设计\03_系统设计\UML导出\SVG
E:\.AA毕业设计\10_项目源码\docs\设计图\导出图\PNG
E:\.AA毕业设计\10_项目源码\docs\设计图\导出图\SVG
```

验证结果：

```text
15 个 .puml 文件全部成功导出 PNG
15 个 .puml 文件全部成功导出 SVG
导出前已清空旧图片目录，避免新旧版本共存
```

### 设计图审查与优化

完成内容：

1. 检查设计图完整性，确认已覆盖用例图、类图、ER 图、组件图、部署图、时序图、活动图、状态图、页面导航图、页面线框图。
2. 删除重复的 `docs/原型设计说明.md`。
3. 优化用例图、核心类图、告警转工单时序图、组件图、部署图、ER 图。
4. 新增 `14_告警状态图.puml` 和 `15_工单状态图.puml`。
5. 重新导出 15 张 PNG 和 15 张 SVG。

规范要求：

1. 除 `开发过程记录.md` 外，设计文件不保留旧版副本。
2. 每次修改 `.puml` 后必须重新导出 PNG/SVG。
3. `docs/设计图` 保持当前唯一最新版。

### 新增开发规则与 AI 接手规范

完成内容：

1. 新增 `docs/开发规则与AI接手规范.md`。
2. 明确后续开发最高原则：先设计，再实现；先验证，再提交。
3. 明确除 `docs/开发过程记录.md` 外，任何文件不允许新旧版本共存。
4. 明确 AI 或新开发者接手前必须阅读的文件。
5. 明确开发记录、开发过程记录、设计图、代码、验证、Git 提交规则。
6. 修复 README 中已删除 `docs/原型设计说明.md` 的旧引用。

涉及文件：

```text
README.md
docs/开发规则与AI接手规范.md
docs/开发文档.md
docs/开发记录.md
```

验证结果：

```text
已确认 README 不再引用已删除的 docs/原型设计说明.md。
新增规则文档用于后续开发和 AI 接手。
```

### 补充完成后复盘与下一步建议规则

完成内容：

1. 在 `docs/开发规则与AI接手规范.md` 中新增“每次完成任务后必须复盘检查，并给出建议执行的下一步”。
2. 将“下一步建议”加入开发记录模板。
3. 新增完成后复盘规则，要求阶段总结包含已完成、验证、风险或遗留、建议下一步。

涉及文件：

```text
docs/开发规则与AI接手规范.md
docs/开发记录.md
```

验证结果：

```text
规则已写入最高原则、开发记录要求和完成后复盘规则。
```

下一步建议：

```text
建议下一步根据当前 ER 图和页面线框图，开始设计设备资产模块的数据表、接口和页面字段，再进入代码实现。
```

### 2026-05-12 第一版运维业务闭环开发

完成内容：

1. 新增后端运维业务接口，覆盖总览、设备资产、监控目标、告警中心、告警转工单、工单列表和 AI 诊断。
2. 新增统一异常处理，支持参数校验失败、非法 JSON、业务参数错误的统一返回。
3. 重写前端工作台页面，形成左侧导航、运维总览、设备资产、监控目标、告警中心、工单看板、AI 诊断六个模块。
4. 正式加入 `@element-plus/icons-vue` 依赖，用 Element Plus 图标完善导航和按钮。
5. 更新 `README.md` 和 `docs/开发文档.md`，同步当前接口和页面能力。

涉及文件：

```text
README.md
backend/src/main/java/com/graduation/ictops/common/ApiResponse.java
backend/src/main/java/com/graduation/ictops/common/GlobalExceptionHandler.java
backend/src/main/java/com/graduation/ictops/ops/OpsController.java
frontend/package.json
frontend/package-lock.json
frontend/src/App.vue
frontend/src/style.css
docs/开发文档.md
docs/开发记录.md
docs/开发过程记录.md
```

验证结果：

```text
使用 set-env.ps1 固定 JDK17 与 Maven 3.9.9 后执行 Maven 测试，通过。
npm run build 通过。
后端已重启到 8080，并验证 /api/health 返回 200。
验证 /api/ops/overview 返回 200。
验证 /api/ops/diagnose 返回 200。
验证 /api/ops/alerts/{alertId}/work-orders 返回 200。
验证非法 JSON 请求返回 400，message 为“请求体不是合法 JSON”。
```

风险或遗留问题：

1. 当前设备、告警、工单数据仍为内存样例数据，后端重启后会恢复初始状态。
2. 当前 AI 诊断为规则化结果，尚未真正接入 FastAPI、大模型 API、向量数据库和知识库。
3. Vite 构建存在 Element Plus 相关的大包体积提示，不影响当前运行，后续可通过路由拆分或手动分包优化。

下一步建议：

```text
建议下一步进入 MySQL 持久化阶段，先实现设备资产、监控目标、告警事件、工单四张核心表，再把当前内存数据迁移为数据库初始化数据和 CRUD 接口。
```

### 2026-05-12 前端视觉升级与角色入口分离

完成内容：

1. 根据新的高保真参考图重构前端视觉风格，使用深色侧栏、浅色工作区、顶部搜索栏、指标卡、表格、看板和右侧详情面板。
2. 将入口拆分为管理员入口和维护人员入口。
3. 管理员端保留总览、监控中心、资源管理、告警中心、工单中心、自动化巡检、报表分析、系统设置。
4. 维护人员端只保留我的工单、告警待办、设备状态、处置记录，避免展示管理员权限功能。
5. 导出 3 张前端视觉稿 PNG，作为后续 UI 实现和论文/答辩材料。

涉及文件：

```text
frontend/src/App.vue
frontend/src/style.css
docs/设计图/前端视觉稿/01_登录分流页.png
docs/设计图/前端视觉稿/02_管理员后台.png
docs/设计图/前端视觉稿/03_维护人员工作台.png
docs/设计图索引.md
docs/开发文档.md
docs/开发记录.md
docs/开发过程记录.md
```

验证结果：

```text
npm run build 通过。
http://127.0.0.1:5173 可访问。
已使用 Edge headless 导出登录分流页、管理员后台、维护人员工作台 3 张截图。
已人工检查截图，修复了维护人员 KPI 文字竖排、首屏密度过高和工单区域拥挤问题。
```

风险或遗留问题：

1. 当前登录仍为前端演示态，尚未接入真实认证、JWT、角色权限和路由守卫。
2. 管理员端与维护人员端目前仍在单个 `App.vue` 中实现，后续功能增加后应拆分组件和路由。
3. 当前图表为 CSS 视觉表达，后续可接入 ECharts 展示真实趋势数据。

下一步建议：

```text
建议下一步先补真实登录与权限模型，增加管理员、维护人员两类角色的后端接口和前端路由，再继续做 MySQL 持久化。
```
